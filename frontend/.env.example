# Frontend local/deployment environment example
# Vercel should define this in the project dashboard for production builds.
VITE_API_URL=http://localhost:3001
# Optional. Use only if media is served from a different host than the API.
VITE_MEDIA_BASE_URL=
VITE_GOOGLE_CLIENT_ID=
